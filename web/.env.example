# Copy to .env

# Browser → API (production: your API domain, no trailing slash)
# Leave empty in dev to use Vite proxy below
VITE_API_URL=

# Vite dev server only
VITE_DEV_PORT=5173
VITE_API_PROXY_TARGET=http://localhost:8080
